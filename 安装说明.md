# Fantacy 审查助手 — 安装与使用

## 安装(一次性,约 1 分钟)

1. 下载本仓库并解压(或 `git clone`),得到文件夹 `fantacy-review-helper`。
2. 打开 Chrome,地址栏输入 `chrome://extensions` 回车。
3. 打开右上角的 **开发者模式** 开关。
4. 点 **加载已解压的扩展程序**,选择上面那个文件夹。
5. 装好后打开 `admin.fantacy.live` 的审查页面即可,屏幕左下角会出现状态小黑条。

> 说明:Chrome 不允许从网页/GitHub 直接一键安装扩展,所以必须走「开发者模式 + 加载文件夹」这一步,这是正常的。

## 用法

### 列表页(字符 / 多字符 tab)
- 鼠标 **悬停** 在某一行上,该行会出现绿色描边高亮。
- 按 **A** = 点该行的 **Pass**(绿色,通过)
- 按 **D** = 点该行的 **Reject**(红色,拒绝)
- 点完后列表会刷新,插件用鼠标当前位置自动重新定位高亮行。

### 详情页
- 按 **A** = 批准(通过,绿色按钮)
- 按 **D** = 拒绝(红色按钮)

### 通用
- 屏幕左下角小黑条显示当前状态和版本号(如 `审查助手 v2.0`),带暂停/继续按钮。
- 在输入框/文本框里打字时 A、D 不会触发,放心填字。
- 想临时停用:回 `chrome://extensions` 关掉这个扩展的开关即可。

## 更新到新版本

- 若用 `git clone` 安装:进文件夹执行 `git pull`,再到 `chrome://extensions` 点该扩展的刷新图标。
- 若下载的 zip:删掉旧文件夹,下载新版解压,重新「加载已解压的扩展程序」。
- 看左下角版本号确认是否为最新。

## 失灵怎么办

如果网站改了按钮样式导致点不中,把最新的按钮 HTML 发给维护者调整选择器即可。
